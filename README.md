WARNING: Never run random scripts by piping them from curl into your shell!
Make sure to read its content first!

``` shell
curl -s "https://codeberg.org/CodeWithMa/dotfiles-installer/raw/branch/dev/install.zsh" | zsh
```
